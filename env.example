import { motion } from 'motion/react';
import { 
  Dna, Sparkles, Target, Compass, 
  ArrowRight, ShieldCheck, Newspaper, Layers
} from 'lucide-react';
import { Link } from 'react-router-dom';

const capabilities = [
  { 
    id: '01', 
    title: 'Profile Diagnostic', 
    desc: 'An exhaustive analysis of academic metrics, curriculum rigor, and extracurricular alignment before strategy begins.',
    icon: Dna,
    path: '/intake'
  },
  { 
    id: '02', 
    title: 'Strategy Studio', 
    desc: 'Developing a unique narrative anchor that differentiates and defines the student in high-competition selection pools.',
    icon: Compass,
    path: '/dashboard'
  },
  { 
    id: '03', 
    title: 'Essay Lab', 
    desc: 'An editorial environment focused on authenticity, voice, and structural elegance. Not just editing—storycrafting.',
    icon: Sparkles,
    path: '/essay-lab'
  },
  { 
    id: '04', 
    title: 'School Fit Explorer', 
    desc: 'A data-informed approach to school selection based on academic targets, cultural fit, and strategic safety.',
    icon: Target,
    path: '/school-fit'
  },
];

export default function Platform() {
  return (
    <div className="min-h-screen bg-paper flex flex-col pt-20">
      {/* Hero Section */}
      <section className="py-24 md:py-32 px-6 border-b border-ink/5">
        <div className="max-w-7xl mx-auto editorial-grid">
           <div className="col-span-12 lg:col-span-8">
              <span className="text-[10px] tracking-[0.4em] uppercase text-bronze mb-6 block">The Ecosystem</span>
              <h1 className="text-5xl md:text-7xl font-serif text-ink tracking-tight mb-8">
                Designed for <span className="italic">outcomes</span>.
              </h1>
              <p className="text-xl text-slate font-light leading-relaxed max-w-2xl">
                Northstar is not just a tool; it is a high-fidelity environment built to optimize every dimension of the admissions journey. From first-day diagnostics to final interview polish.
              </p>
           </div>
        </div>
      </section>

      {/* Capabilities Grid */}
      <section className="py-32 px-6">
        <div className="max-w-7xl mx-auto">
           <div className="editorial-grid gap-y-24">
              {capabilities.map((item) => (
                <div key={item.id} className="col-span-12 md:col-span-6 lg:col-span-6 group cursor-pointer">
                   <div className="editorial-grid gap-8">
                      <div className="col-span-12 lg:col-span-1">
                         <span className="text-2xl font-serif text-bronze/40 italic">{item.id}</span>
                      </div>
                      <div className="col-span-12 lg:col-span-11 space-y-8">
                         <div className="w-16 h-16 bg-ink/2 flex items-center justify-center rounded-sm group-hover:bg-forest/5 transition-colors">
                            <item.icon size={28} className="text-slate group-hover:text-forest transition-colors" />
                         </div>
                         <h2 className="text-3xl font-serif text-ink">{item.title}</h2>
                         <p className="text-slate font-light leading-relaxed max-w-md">{item.desc}</p>
                         <Link to={item.path} className="inline-flex items-center gap-2 text-forest font-bold text-[10px] uppercase tracking-widest hover:translate-x-1 transition-all">
                            Enter Module <ArrowRight size={14} />
                         </Link>
                      </div>
                   </div>
                </div>
              ))}
           </div>
        </div>
      </section>

      {/* Philosophy Section */}
      <section className="py-32 bg-ink text-paper px-6 overflow-hidden">
         <div className="max-w-7xl mx-auto editorial-grid gap-16">
            <div className="col-span-12 lg:col-span-5 relative">
               <motion.div 
                 initial={{ opacity: 0, scale: 0.8 }}
                 whileInView={{ opacity: 1, scale: 1 }}
                 className="aspect-square bg-paper/5 border border-paper/10 flex items-center justify-center rounded-sm"
               >
                  <ShieldCheck size={120} className="text-bronze/20" />
               </motion.div>
            </div>
            <div className="col-span-12 lg:col-span-7 flex flex-col justify-center">
               <span className="text-[10px] tracking-[0.4em] uppercase text-bronze mb-8 block">Our Logic</span>
               <h2 className="text-4xl font-serif mb-10 italic">The boutique advisor at scale.</h2>
               <div className="space-y-12">
                  <div className="flex gap-8">
                     <div className="w-px h-12 bg-bronze/40" />
                     <div>
                        <h4 className="text-xl font-serif mb-3">Authenticity Protocol</h4>
                        <p className="text-sm font-light text-paper/60 leading-relaxed max-w-lg">
                           We prioritize the student’s authentic voice over formulaic responses. Our systems are built to detect and remove cliches that dilute personal impact.
                        </p>
                     </div>
                  </div>
                  <div className="flex gap-8">
                     <div className="w-px h-12 bg-bronze/40" />
                     <div>
                        <h4 className="text-xl font-serif mb-3">Strategic Readiness</h4>
                        <p className="text-sm font-light text-paper/60 leading-relaxed max-w-lg">
                           Admissions is a game of probability. We provide the analytics necessary to understand where you are competitive and where you are vulnerable.
                        </p>
                     </div>
                  </div>
                  <div className="flex gap-8">
                     <div className="w-px h-12 bg-bronze/40" />
                     <div>
                        <h4 className="text-xl font-serif mb-3">Professional Standards</h4>
                        <p className="text-sm font-light text-paper/60 leading-relaxed max-w-lg">
                           The Northstar workspace follows the same rigors as a high-end educational consultancy—offering the same polish and precision.
                        </p>
                     </div>
                  </div>
               </div>
            </div>
         </div>
      </section>

      {/* Integration Logos or Partnerships (Mock) */}
      <section className="py-24 px-6 border-t border-ink/5">
        <div className="max-w-7xl mx-auto flex flex-col items-center">
           <span className="text-[10px] tracking-[0.3em] uppercase text-slate/40 mb-12 font-bold italic">Standard Integrations</span>
           <div className="flex flex-wrap justify-between items-center w-full max-w-5xl opacity-40 grayscale gap-12">
              <span className="text-2xl font-serif">The Chronicle</span>
              <span className="text-2xl font-serif italic font-bold">Academic View</span>
              <span className="text-2xl font-serif font-light">IvyTracker</span>
              <span className="text-2xl font-serif italic">Global Admissions</span>
           </div>
        </div>
      </section>
    </div>
  );
}
