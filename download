import { motion } from 'motion/react';
import { 
  Mic2, MessageCircle, PlayCircle, Star, 
  HelpCircle, CheckCircle2, AlertCircle, Quote
} from 'lucide-react';
import { useState } from 'react';

const questions = [
  { id: 1, type: 'Personal', text: 'Tell me about yourself beyond your academic record.', count: 12 },
  { id: 2, type: 'Academic', text: 'How do you plan to contribute to our intellectual community?', count: 8 },
  { id: 3, type: 'Critical', text: 'Describe a significant failure and what you learned from the reflection.', count: 15 },
  { id: 4, type: 'Vision', text: 'Where do you see your primary research interests in five years?', count: 6 },
];

export default function InterviewPrep() {
  const [activeQ, setActiveQ] = useState(questions[0]);

  return (
    <div className="min-h-screen bg-paper flex flex-col pt-20">
      <div className="max-w-7xl mx-auto w-full px-6 py-20 flex-grow">
        <div className="flex flex-col md:flex-row justify-between items-start md:items-end mb-20 gap-8">
           <div>
              <span className="text-[10px] tracking-[0.4em] uppercase text-bronze mb-2 block">Voice & Presence</span>
              <h1 className="text-4xl lg:text-5xl font-serif text-ink tracking-tight">The <span className="italic">Candidate</span> voice.</h1>
           </div>
           
           <div className="flex gap-4">
              <button className="bg-forest text-paper px-8 py-4 text-[10px] uppercase tracking-widest font-bold hover:bg-forest/90 transition-all rounded-sm flex items-center gap-3 shadow-subtle group">
                 <PlayCircle size={16} /> Begin Mock Session
              </button>
           </div>
        </div>

        <div className="editorial-grid gap-12">
          {/* Question Hub */}
          <div className="col-span-12 lg:col-span-4 space-y-6">
             <h3 className="text-[10px] uppercase tracking-[0.3em] text-slate font-bold mb-8">Question Protocol</h3>
             {questions.map((q) => (
               <div 
                 key={q.id}
                 onClick={() => setActiveQ(q)}
                 className={`p-8 border rounded-sm cursor-pointer transition-all ${activeQ.id === q.id ? 'bg-white border-forest shadow-lg translate-x-1' : 'bg-white/50 border-ink/5 hover:border-ink/20'}`}
               >
                  <div className="flex justify-between items-center mb-4">
                     <span className="text-[10px] uppercase tracking-widest font-bold text-bronze">{q.type}</span>
                     <span className="text-[10px] text-slate/40 italic font-serif ">{q.count} Practice Hits</span>
                  </div>
                  <p className="font-serif text-lg text-ink leading-tight">{q.text}</p>
               </div>
             ))}
             
             <div className="p-8 bg-ink text-paper rounded-sm mt-12">
                <h4 className="text-[10px] uppercase tracking-widest text-bronze font-bold mb-6 italic">The Rubric</h4>
                <ul className="space-y-4">
                   {['Articulation', 'Confidence', 'Clarity of Field', 'Growth Mindset'].map((item) => (
                     <li key={item} className="flex justify-between items-center border-b border-paper/10 pb-2">
                        <span className="text-xs font-light text-paper/70 tracking-wide">{item}</span>
                        <div className="flex gap-1">
                           {[1, 2, 3, 4, 5].map((s) => (
                             <div key={s} className={`w-1 h-1 rounded-full ${s <= 4 ? 'bg-bronze' : 'bg-paper/20'}`} />
                           ))}
                        </div>
                     </li>
                   ))}
                </ul>
             </div>
          </div>

          {/* Active Work Area */}
          <div className="col-span-12 lg:col-span-8 flex flex-col gap-8">
             <div className="bg-white border border-ink/10 rounded-sm p-12 md:p-20 shadow-subtle relative aspect-video flex flex-col items-center justify-center text-center overflow-hidden">
                {/* Simulated Camera Viewfinder */}
                <div className="absolute top-8 left-8 flex gap-4">
                   <div className="flex items-center gap-2 group cursor-pointer">
                      <div className="w-2 h-2 rounded-full bg-oxblood animate-pulse" />
                      <span className="text-[10px] uppercase tracking-widest text-ink font-bold opacity-60">Rec</span>
                   </div>
                </div>
                <div className="absolute top-8 right-8 flex gap-4">
                   <Mic2 size={16} className="text-forest animate-bounce" />
                </div>

                <div className="space-y-10 max-w-xl z-10">
                   <Quote className="w-12 h-12 text-bronze/20 mx-auto mb-4" />
                   <h2 className="text-3xl md:text-5xl font-serif text-ink tracking-tight italic">
                     {activeQ.text}
                   </h2>
                   <p className="text-xs text-slate tracking-[0.2em] uppercase font-bold">Waiting for your response...</p>
                </div>
                
                {/* Decorative grain/scanlines */}
                <div className="absolute inset-0 pointer-events-none bg-paper/5 opacity-40 mix-blend-overlay" />
             </div>

             <div className="editorial-grid gap-8">
                <div className="col-span-12 md:col-span-6 p-10 bg-forest text-paper rounded-sm">
                   <h3 className="text-[10px] uppercase tracking-[0.4em] text-bronze mb-8 font-bold">Peer Insight</h3>
                   <div className="space-y-6">
                      <p className="text-sm font-light leading-relaxed italic text-paper/80">
                        "In your last attempt, you spent 2:40 on the intro—consider reducing to 1:15 to allow for more specific academic evidence later."
                      </p>
                      <div className="flex items-center gap-3 pt-6 border-t border-paper/10">
                         <div className="w-8 h-8 bg-paper/10 rounded-full flex items-center justify-center font-serif text-xs">M</div>
                         <p className="text-[10px] tracking-widest uppercase text-bronze font-bold">Advising Note • 2h ago</p>
                      </div>
                   </div>
                </div>

                <div className="col-span-12 md:col-span-6 p-10 border border-ink/10 bg-white rounded-sm shadow-subtle space-y-8">
                   <h3 className="text-[10px] uppercase tracking-[0.2em] text-slate font-bold">Feedback Loop</h3>
                   <div className="space-y-6">
                      {[
                        { label: 'Voice Modulation', val: 'Consistent', status: 'pass' },
                        { label: 'Eye Contact Ratio', val: 'Low (68%)', status: 'warn' },
                        { label: 'Wait Time', val: 'Optimal (2s)', status: 'pass' },
                      ].map((f) => (
                        <div key={f.label} className="flex justify-between items-center bg-paper/50 p-4 rounded-sm border border-ink/5">
                           <div>
                              <p className="text-[9px] uppercase tracking-widest text-slate/40 mb-1 font-bold">{f.label}</p>
                              <p className="text-sm font-serif">{f.val}</p>
                           </div>
                           {f.status === 'pass' ? <CheckCircle2 className="text-forest w-5 h-5" /> : <AlertCircle className="text-oxblood w-5 h-5" />}
                        </div>
                      ))}
                   </div>
                </div>
             </div>
          </div>
        </div>
      </div>
    </div>
  );
}
